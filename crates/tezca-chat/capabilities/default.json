{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The panel draws its own chrome, so it needs the window commands the decorations would have provided. Clipboard write is the code-block copy button.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
